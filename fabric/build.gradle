plugins {
    id 'multiloader-loader'
    id 'fabric-loom'
}
// Fabric 模块直接编译公共源码，因此要提供 Minecraft、官方映射、Fabric Loader/API 以及公共源码用到的注解。
dependencies {
    minecraft "com.mojang:minecraft:${minecraft_version}"
    mappings loom.officialMojangMappings()
    modImplementation "net.fabricmc:fabric-loader:${fabric_loader_version}"
    modImplementation "net.fabricmc.fabric-api:fabric-api:${fabric_version}"
    // Loader compilation includes common sources directly, so keep this on Fabric's classpath too.
    compileOnly "com.google.code.findbugs:jsr305:3.0.2"
}

// 配置可选访问扩展、Mixin 映射文件，以及开发时的客户端／服务器启动方式。服务器启动项不表示 Mod 会在服务端执行客户端入口。
loom {
    def aw = project(':common').file("src/main/resources/${mod_id}.accesswidener")
    if (aw.exists()) {
        accessWidenerPath.set(aw)
    }
    mixin {
        defaultRefmapName.set("${mod_id}.refmap.json")
    }
    runs {
        client {
            client()
            setConfigName('Fabric Client')
            ideConfigGenerated(true)
            runDir('runs/client')
            vmArg '-Xmx3072M'
            programArgs '--width', '1280', '--height', '720'
        }
        server {
            server()
            setConfigName('Fabric Server')
            ideConfigGenerated(true)
            runDir('runs/server')
        }
    }
}
